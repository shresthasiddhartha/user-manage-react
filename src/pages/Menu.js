const Menu =()=> {
    const pageTitle='menu'
    return(
            <div>
                {/* html codes */}
                {pageTitle}
                <h1> this is menu content

                </h1>
            </div>
        );
}

export default Menu;